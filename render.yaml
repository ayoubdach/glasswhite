services:
  - type: worker
    name: alpha-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python alpha_bot.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
        
